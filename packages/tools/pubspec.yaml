name: tools
description: >-
  Tools used during development.

version: 0.1.0
publish_to: none

executables:
  buf:
  license_header:

environment:
  sdk: ^3.7.0
resolution: workspace
dependencies:
  path: ^1.9.1
